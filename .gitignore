node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json

# Notifier runtime state (ephemeral — recreated on start)
scripts/notifier_state.json

# Local Claude settings (not shared)
.claude/settings.local.json

# DigitalOcean App Platform spec (contains secrets — never commit)
.do/app.yaml
